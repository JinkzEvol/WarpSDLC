# Initial benched primitive manifest

Skills expected to bench in many hosts unless a matching runtime exists:

- `warp-integration-test-san`
- `integration-test-video-san`
- `warp-ui-guidelines-san`
- `rust-unit-tests-san` in non-Rust hosts
- `triage-issue-local-san` until a core issue primitive exists
- `dedupe-issue-local-san` until a core issue primitive exists

Workflow families likely to bench:

- local `.warp/workflows/` helpers that assume Warp app launch or local machine
  setup
- workflow refresh jobs that depend on external `oz-for-oss` automation